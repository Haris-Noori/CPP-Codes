#include <iostream>
using namespace std;

int count_digits(int n)
{
  if( (-10 < n) && (n < 10) )
  {  return 1;  }
  else
  {  return 1 + count_digits(n/10);  }
}

int main() 
{
  cout << count_digits(55);
}
